<div class="container">
    <div class="row">
      <h1>
        <%= @user.name %>
      </h1>
    </div>
</div>
<div class="container" id="reviewsContainer">
  <% if @user.reviews.any? %>
    <%= render @reviews %>
    <%= will_paginate @reviews %>
  <% end %>
</div>